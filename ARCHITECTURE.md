# VectaHub 系统架构设计文档

## 1. 概述

VectaHub 是一款面向个人开发者的 CLI 工作流引擎，支持自然语言到工作流的转换与执行。本文档描述系统的整体架构设计，包括模块划分、交互关系、技术选型和设计原则。

---

## 2. 架构目标

| 目标 | 描述 |
|------|------|
| **可扩展性** | 支持插件化架构，便于功能扩展 |
| **高性能** | 低延迟响应，支持并发执行 |
| **安全性** | 沙箱隔离，命令审计，权限控制 |
| **易用性** | 自然语言交互，智能补全，友好输出 |
| **可观测性** | 性能监控，日志追踪，调试支持 |

---

## 3. 系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户层 (User Layer)                         │
│  CLI Interface │ REST API │ VSCode Extension │ Web Dashboard       │
└───────────────────┬─────────────────────────────────────────────────┘
                    │
┌───────────────────▼─────────────────────────────────────────────────┐
│                      应用层 (Application Layer)                     │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │  CLI Core   │ │  API Server │ │  Plugin Mgmt│ │  Monitor    │   │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘   │
└─────────┼───────────────┼───────────────┼───────────────┼───────────┘
          │               │               │               │
┌─────────▼───────────────▼───────────────▼───────────────▼───────────┐
│                      业务层 (Business Layer)                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │ NL Parser   │ │ Workflow    │ │ Skill Exec  │ │ Debugger    │   │
│  │  (Intent    │ │  Engine     │ │  (Skills)   │ │  (Debug)    │   │
│  │   Matcher)  │ │             │ │             │ │             │   │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘   │
│         │               │               │               │           │
│  ┌──────▼──────┐       │               │               │           │
│  │  LLM Client │───────┘               │               │           │
│  └─────────────┘                       │               │           │
└────────────────────────────────────────┼───────────────┼───────────┘
                                        │               │
┌────────────────────────────────────────▼───────────────▼───────────┐
│                      基础设施层 (Infrastructure Layer)              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │  Security   │ │  Storage    │ │  Sandbox    │ │  Audit      │   │
│  │  Protocol   │ │  (Disk/DB)  │ │  (Isolation)│ │  Logging    │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4. 模块划分与职责

### 4.1 用户层

| 模块 | 职责 | 技术栈 |
|------|------|--------|
| **CLI Interface** | 命令行交互入口，参数解析 | Commander.js |
| **REST API** | HTTP接口服务 | Express.js |
| **VSCode Extension** | IDE集成插件 | VSCode API |
| **Web Dashboard** | 可视化管理界面 | React/Vue |

### 4.2 应用层

| 模块 | 职责 | 关键功能 |
|------|------|----------|
| **CLI Core** | 命令路由，懒加载，帮助生成 | 命令注册、子命令分发 |
| **API Server** | HTTP请求处理，认证授权 | RESTful接口、WebSocket |
| **Plugin Manager** | 插件生命周期管理 | 加载、激活、卸载、更新 |
| **Performance Monitor** | 指标采集，告警通知 | CPU/内存监控、阈值告警 |

### 4.3 业务层

| 模块 | 职责 | 关键功能 |
|------|------|----------|
| **NL Parser** | 自然语言解析，意图识别 | LLM调用、关键词匹配、参数提取 |
| **Workflow Engine** | 工作流编排与执行 | DAG调度、并行执行、状态管理 |
| **Skill Executor** | 技能执行框架 | 命令技能、意图技能、工作流技能 |
| **Debugger** | 工作流调试支持 | 断点、单步执行、变量监视 |
| **LLM Client** | AI模型调用封装 | 多提供商支持、缓存、流式响应 |

### 4.4 基础设施层

| 模块 | 职责 | 关键功能 |
|------|------|----------|
| **Security Protocol** | 安全策略管理 | RBAC、命令审计、危险检测 |
| **Storage** | 数据持久化 | 工作流存储、配置管理、历史记录 |
| **Sandbox** | 命令执行隔离 | 资源限制、危险命令检测 |
| **Audit Logging** | 操作审计 | 日志记录、安全事件追踪 |

---

## 5. 核心数据流

### 5.1 命令执行流程

```
用户输入 → CLI解析 → 命令路由 → 命令执行 → 结果输出
              │              │
              ▼              ▼
         意图识别        安全检查
              │              │
              └──────┬───────┘
                     ▼
              沙箱隔离执行
```

### 5.2 自然语言工作流流程

```
用户输入 → NL Parser → 意图匹配 → LLM增强 → 工作流生成 → 执行
              │                    │
              ▼                    ▼
         关键词提取           参数提取
```

### 5.3 插件加载流程

```
启动 → 配置读取 → 插件发现 → 依赖检查 → 插件加载 → 激活 → 注册命令
                              │
                              ▼
                         沙箱隔离
```

---

## 6. 技术选型

### 6.1 核心技术栈

| 分类 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| **语言** | TypeScript | 5.x | 类型安全、成熟生态、IDE支持 |
| **运行时** | Node.js | 20.x | 高性能、异步IO、NPM生态 |
| **CLI框架** | Commander.js | 12.x | 成熟稳定、功能完善 |
| **测试框架** | Vitest | 2.x | 快速、内置mock、TypeScript支持 |

### 6.2 第三方服务

| 服务 | 用途 | 支持提供商 |
|------|------|-----------|
| **LLM API** | 自然语言处理 | OpenAI、Anthropic、Ollama、Groq |
| **向量数据库** | 语义搜索 | 可选集成 |

---

## 7. 设计原则

### 7.1 SOLID原则

- **单一职责**：每个模块只负责一个核心功能
- **开闭原则**：通过插件机制扩展，不修改核心代码
- **依赖倒置**：依赖抽象接口而非具体实现

### 7.2 架构原则

- **模块化**：高内聚低耦合，模块间通过接口通信
- **可扩展性**：插件化设计，支持热插拔
- **安全性**：默认安全，沙箱隔离所有外部命令
- **可观测性**：内置监控、日志、调试能力

---

## 8. 关键接口定义

### 8.1 命令接口

```typescript
interface CommandDefinition {
  name: string;
  description: string;
  args: Array<{ name: string; required: boolean }>;
  options: Array<{ name: string; type: 'string' | 'boolean' | 'number' }>;
  action: (args: Record<string, unknown>, options: Record<string, unknown>) => Promise<void>;
}
```

### 8.2 插件接口

```typescript
interface PluginManifest {
  id: string;
  name: string;
  version: string;
  description: string;
  commands?: CommandDefinition[];
  hooks?: Array<{ name: string; description?: string }>;
}

interface PluginInstance {
  manifest: PluginManifest;
  activate: (context: PluginContext) => Promise<void>;
  deactivate: () => Promise<void>;
}
```

### 8.3 工作流接口

```typescript
interface WorkflowStep {
  id: string;
  name: string;
  type: 'exec' | 'if' | 'for_each' | 'parallel';
  cli?: string;
  args?: string[];
  condition?: string;
  items?: string;
  body?: WorkflowStep[];
}

interface Workflow {
  id: string;
  name: string;
  version: string;
  steps: WorkflowStep[];
  variables?: Record<string, unknown>;
}
```

---

## 9. 部署与集成

### 9.1 部署方式

| 环境 | 部署方式 |
|------|---------|
| **开发** | 本地Node.js运行 |
| **生产** | npm全局安装 |
| **容器** | Docker镜像 |

### 9.2 配置管理

配置优先级（从高到低）：
1. 命令行参数
2. 环境变量
3. 用户配置文件 (`~/.vectahub/config.json`)
4. 默认配置

---

## 10. 安全性设计

### 10.1 安全边界

| 边界 | 防护措施 |
|------|---------|
| **命令执行** | 沙箱隔离、危险命令黑名单 |
| **插件加载** | 代码签名验证、权限检查 |
| **数据存储** | 敏感信息加密、访问控制 |
| **网络通信** | HTTPS、API密钥管理 |

### 10.2 安全审计

- 所有命令执行记录审计日志
- 危险操作自动告警
- 支持审计日志导出

---

## 11. 监控与运维

### 11.1 监控指标

| 指标类型 | 具体指标 |
|----------|---------|
| **性能** | CPU使用率、内存占用、响应时间 |
| **可用性** | 命令成功率、错误率 |
| **资源** | 工作流执行次数、插件安装数 |

### 11.2 日志体系

- **INFO**：常规操作记录
- **WARN**：潜在问题警告
- **ERROR**：错误信息
- **DEBUG**：调试详细信息

---

## 12. 版本演进

| 版本 | 重点功能 | 时间线 |
|------|---------|--------|
| **v1.0** | 核心CLI、工作流引擎、基础NL支持 | Q1 2026 |
| **v1.1** | 插件系统、命令补全 | Q2 2026 |
| **v1.2** | 性能监控、工作流调试器 | Q2 2026 |
| **v2.0** | VSCode插件、Web Dashboard | Q3 2026 |

---

**文档版本**: v1.0  
**创建日期**: 2026-05-06  
**作者**: VectaHub Architecture Team