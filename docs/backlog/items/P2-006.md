# P2-006: Checkpoint Reference Policy

> Status: done
> Priority: P2
> Source: Architecture alignment
> Module: `src/orchestration-plan/checkpoint-reference-validator.ts`, `src/types/checkpoint-reference.ts`
> Completed: 2026-06-08

## Description

Recovery and worker-native resume flows need a validated checkpoint-reference policy so git, worktree, and worker-native checkpoint records can be checked before reuse.

The validator and availability checks were already implemented and tested, but the backlog item file was missing.

## Expected

- Validate checkpoint reference shapes for git, worktree, and worker-native checkpoints.
- Check worktree checkpoint availability.
- Expose a dispatch layer for checkpoint availability checks.
- Default worker-native checks to conservative availability metadata.

## Scope

- In-scope: checkpoint reference validation
- In-scope: worktree availability checking
- In-scope: availability dispatch by checkpoint type
- Out-of-scope: deep git graph validation
- Out-of-scope: worker-specific checkpoint probing integrations

## Verification Plan

1. Valid checkpoint references pass validation.
2. Invalid checkpoint references return meaningful errors.
3. Worktree availability reflects actual filesystem presence.
4. Availability dispatch works across checkpoint types.

## Completion

### Changes

- `src/orchestration-plan/checkpoint-reference-validator.ts`:
  - validates checkpoint references and checks availability by checkpoint type
- `src/orchestration-plan/checkpoint-reference-validator.test.ts`:
  - verifies valid/invalid references and availability behavior

### Verification Evidence

- Tests: `npx vitest run src/orchestration-plan/checkpoint-reference-validator.test.ts` passed
- Typecheck: `npm run typecheck` passed
- Lint: `npm run lint` passed
