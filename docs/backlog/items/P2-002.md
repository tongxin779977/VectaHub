# P2-002: Worker Capability Matrix

> Status: done
> Priority: P2
> Source: Architecture alignment
> Module: `src/orchestration-plan/worker-capability-matrix.ts`, `src/types/worker-capability.ts`
> Completed: 2026-06-08

## Description

Delegation and agent-runtime decisions need a normalized worker capability matrix describing supported native features, suitable task types, execution constraints, and whether a worker can appear in executable plans.

The matrix builder and tests were already implemented, but the backlog item file was missing.

## Expected

- Build a capability matrix for built-in and registered workers.
- Expose native feature support levels.
- Expose suitable task types and execution constraints.
- Expose executable-plan eligibility per worker.

## Scope

- In-scope: built-in worker capability summaries
- In-scope: registry-backed worker inclusion
- In-scope: feature/task suitability helpers
- Out-of-scope: remote worker discovery
- Out-of-scope: runtime health probing of external CLIs

## Verification Plan

1. Built-in workers appear in the matrix with normalized summaries.
2. Feature support queries return supported/partial/unsupported as expected.
3. Task suitability and executable-plan eligibility helpers behave correctly.
4. Focused tests and static checks pass.

## Completion

### Changes

- `src/orchestration-plan/worker-capability-matrix.ts`:
  - builds worker capability summaries and helper queries for built-in and registered workers
- `src/orchestration-plan/worker-capability-matrix.test.ts`:
  - verifies matrix contents, feature support, suitable tasks, constraints, and executable-plan eligibility

### Verification Evidence

- Tests: `npx vitest run src/orchestration-plan/worker-capability-matrix.test.ts` passed
- Typecheck: `npm run typecheck` passed
- Lint: `npm run lint` passed
