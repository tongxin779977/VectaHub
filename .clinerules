# VectaHub Project Rules

> Natural Language Workflow Engine — TypeScript/Node.js CLI, ESM, 1185+ tests.

## Project Context

| Area | Path |
|------|------|
| CLI entry | `src/cli.ts` |
| Command wiring | `src/commands/module.ts` |
| All commands | `src/commands/` |
| Shared types | `src/types/index.ts` |
| Workflow engine | `src/workflow/` |
| NL/LLM engine | `src/nl/` |
| Skills runtime | `src/skills/` |
| Sandbox/security | `src/sandbox/`, `src/security-protocol/`, `src/command-rules/`, `src/cli-tools/` |
| Audit/logging | `src/infrastructure/audit/`, `src/utils/audit.ts` |
| Setup/config | `src/setup/`, `src/infrastructure/config/`, `src/utils/config.ts`, `src/utils/paths.ts` |
| Chat interface | `src/chat/` |
| Daemon | `src/daemon/` |
| Monitoring | `src/monitoring/` |
| Debugger | `src/debugger/` |
| VS Code extension | `packages/vectahub-vscode-extension/src/` |
| Templates | `templates/` |
| Tests | Colocated: `src/foo.ts` → `src/foo.test.ts` |

## Module Map

| Task | Start With |
|------|-----------|
| CLI command bug | `src/commands/`, then `src/cli.ts`, `src/commands/module.ts` |
| Workflow behavior | `src/workflow/`, `src/types/index.ts` |
| Natural language / LLM | `src/nl/`, `src/skills/` |
| Sandbox / dangerous command | `src/sandbox/`, `src/security-protocol/`, `src/command-rules/`, `src/cli-tools/` |
| Audit / logging | `src/infrastructure/audit/`, `src/utils/audit.ts` |
| Setup / config | `src/setup/`, `src/infrastructure/config/`, `src/utils/config.ts`, `src/utils/paths.ts` |
| VS Code extension | `packages/vectahub-vscode-extension/src/` |
| Chat | `src/chat/` |
| Debug / diagnostics | `src/debugger/`, `src/commands/doctor.ts`, `src/commands/debug.ts` |

## Hard Stops

- Evidence only: no tool run, no success claim.
- Do not claim tests pass unless a real check passed.
- After code changes, run relevant verification.
- Do not fabricate files, APIs, IDE features, or command output.
- Do not bypass sandbox, validation, or user confirmation.
- Do not read, print, log, or commit secrets/tokens/keys/`.env`.
- Use `getVectaHubPath()` for VectaHub home/config/data paths; use context-specific paths for tests and IDE storage.
- Commit format: `[module] short description`, <=50 chars, no period.

## Code Style

- **ESM only**: imports use `.js` extension (`import { foo } from './bar.js'`)
- **TypeScript 5.7**, Node.js >= 21.0.0
- **Factory pattern**: `createXxx()` functions, single responsibility
- **Types first**: define interface/type before implementation
- **No default exports**: use named exports
- **Lazy loading**: CLI commands use dynamic `import()` on first invocation
- **Commander.js** for CLI: each command exports a `Command` object
- **Vitest** for tests: colocated `*.test.ts` files

## Verify Loop

- Run the narrowest relevant check first.
- Passing checks: summarize command and result.
- Failing checks: show key raw error and stop before broad retries.

## Verification Commands

| Command | Purpose |
|---------|---------|
| `npm run typecheck` | Type checking |
| `npm test` | Full test suite (watch mode) |
| `npm run test:run` | One-shot test run (CI mode) |
| `npm run build` | Build with tsup |
| `npm run dev -- verify --type <typecheck\|test\|coverage\|all>` | CLI verification |
| `npm run dev -- doctor` | Full diagnostics |
| `npm run dev -- security list` | Security rules |
| `npm run dev -- security test -- <cmd>` | Test command safety |
| `npm run dev -- audit list --limit 20` | Recent audit logs |
| `npm run dev -- audit query --event <type>` | Query by event type |
| `npm run dev -- audit stats` | Audit statistics |

## Task Template

```
Goal:
Observed behavior:
Expected behavior:
Scope:
Do not change:
Allowed:
Verification:
Direct edit or plan first:
```

- If scope is unclear, ask one blocking question.
- If user says analysis/plan, do not edit.
- If user says direct edit, make the smallest safe change.
- After code changes, run relevant verification.

## Detailed Skill References

When working on specific modules, read the corresponding skill file for detailed guidance:

| Scenario | Read This File |
|----------|---------------|
| Workflow engine: DAG, executor, expressions, storage | `.cline/skills/vectahub-workflow.md` |
| NL/LLM: intents, tool calling, prompts, sessions | `.cline/skills/vectahub-nl.md` |
| Adding/modifying CLI commands | `.cline/skills/vectahub-cli.md` |
| Sandbox, security protocol, audit changes | `.cline/skills/vectahub-security.md` |
| Debugging, diagnostics, doctor, audit queries | `.cline/skills/vectahub-debug.md` |